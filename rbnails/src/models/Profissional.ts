// import mongoose, { Schema, Document, models, model } from 'mongoose';

// export interface IProfissional extends Document {
//   nome: string;
//   ativo: boolean;
// }

// const ProfissionalSchema: Schema = new Schema({
//   nome: {
//     type: String,
//     required: [true, 'O nome do profissional é obrigatório.'],
//     trim: true,
//   },
//   ativo: {
//     type: Boolean,
//     default: true,
//   },
// });

// export default models.Profissional || model<IProfissional>('Profissional', ProfissionalSchema);
